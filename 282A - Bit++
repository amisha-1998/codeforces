#include<iostream>
using namespace std;
int main()
{
	int X=0;
	int n;
	cin>>n;
	string str;
	while(n--)
	{
		cin>>str;
		if(str == "X++"|| str == "++X")
		{
			X=X+1;
		}
		if(str == "X--"|| str == "--X")
		{
			X=X-1;
		}
	}
	cout<<X<<endl;
	return 0;
}
